/*! JSZip v3.10.1 - (c) 2009-2022 David Duponchel and contributors (https://github.com/Stuk/jszip) - MIT License */
(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define([], factory);
  } else if (typeof exports === "object") {
    module.exports = factory();
  } else {
    global.JSZip = factory();
  }
}(this, (function () {
  "use strict";

  function JSZip() {
    // Initialization code here
  }

  // Add methods to JSZip prototype
  JSZip.prototype.loadAsync = function (data) {
    // Loading logic
  };

  JSZip.prototype.file = function (name, content) {
    // File addition logic
  };

  // Other methods...

  return JSZip;
})));
